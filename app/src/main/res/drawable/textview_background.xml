<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/SplashBackground"/>
    <!-- this one is ths color of the Rounded Button -->
    <stroke
        android:width="1dp"
        android:color="@color/SplashBackground" />
    <solid android:color="@android:color/transparent"/>
    <corners
        android:radius="@dimen/dim_1"
        android:bottomLeftRadius="@dimen/dim_20"
        android:bottomRightRadius="@dimen/dim_20"
        android:topLeftRadius="@dimen/dim_20"
        android:topRightRadius="@dimen/dim_20">

    </corners>
</shape>